name: Test Automation

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 자동화 실행 확인
        run: |
          echo "자동화 실행 시작"
          echo "GitHub Actions 테스트 성공"
          echo "자동화 실행 완료"
